import { ConfirmButtonDirective } from './confirm-button.directive';

describe('ConfirmationalButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmButtonDirective();
    expect(directive).toBeTruthy();
  });
});
